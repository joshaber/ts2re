{
  "name": "ts2re",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "convert": "node out/main.js",
    "fixtures": "npm run build && npm run convert -- fixtures/request.d.ts && npm run convert -- fixtures/electron.d.ts && npm run convert -- fixtures/moment.d.ts"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "camelcase": "^4.1.0"
  },
  "devDependencies": {
    "@types/node": "^7.0.14",
    "typescript": "^2.4.1"
  }
}
